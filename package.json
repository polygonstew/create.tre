{
  "name": "create-tre",
  "displayName": "create.tre",
  "description": "create files and folders from a tree text",
  "version": "0.0.1",
  "publisher": "polygonstew",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "createTre.testParser",
        "title": "create.tre: Test Parser"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/vscode": "^1.80.0",
    "@types/node": "^20.0.0"
  }
}